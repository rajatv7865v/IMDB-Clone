import React from 'react';
import ReactLoading from 'react-loading';
 
const Loading = () => (
    <ReactLoading type='spin' color='white' height={200} width={30} />
);
 
export default Loading;